$(document).ready(function() {    // AJAX Call to Fetch Monthly Sales Data    $.ajax({        url: '../controllers/salesController.php',        type: 'POST',        dataType: 'json',        data: {action: 'GetMonthly'},        success: function (res) {            // Prepare data for the chart            const labels = [];            const salesData = [];            res.forEach(item => {                labels.push(getMonthName(parseInt(item.sale_month))); // Convert month number to name                salesData.push(parseInt(item.total_sales)); // Ensure data is a number            });            // Update chart with the fetched data            createChart(labels, salesData);        },        error: function (xhr, status, error) {            console.error('Error:', error); // Log errors        }    });    // Helper function to convert month number to name    function getMonthName(monthNumber) {        const monthNames = [            "January", "February", "March", "April", "May", "June",            "July", "August", "September", "October", "November", "December"        ];        return monthNames[monthNumber - 1];    }    // Function to create the chart    function createChart(labels, salesData) {        const ctx = document.getElementById('salesController-chart').getContext('2d');        new Chart(ctx, {            type: 'bar',            data: {                labels: labels,                datasets: [{                    label: 'Monthly Sales',                    backgroundColor: '#1c3f7c',                    data: salesData,                }]            },            options: {                plugins: {                    legend: {                        display: false                    }                },                scales: {                    y: {                        beginAtZero: true                    }                }            }        });    }    // chart for every dayday sales    $.ajax({        url: '../controllers/salesController.php',        type: 'POST',        dataType: 'json',        data: { action: 'GetDaily' },        success: function (res) {            const days = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];            const salesData = new Array(7).fill(0); // Initialize an array to hold sales data for each day            res.forEach(sale => {                const dayIndex = sale.sale_day - 1; // Map 1 (Sunday) to 0, 2 (Monday) to 1, and so on                salesData[dayIndex] = sale.total_sales; // Assign sales data to the respective day index            });            const SalesAaday = new Chart($('#per-day-salesController'), {                type: 'doughnut',                data: {                    labels: days, // Days of the week                    datasets: [{                        backgroundColor: ['#6610f2', '#198754', '#ffc107', '#dc3545', '#0dcaf0', '#fd7e14', '#6c757d'], // Colors for each day                        data: salesData, // Sales data for each day                    }]                },                options: {                    responsive: true,                    plugins: {                        legend: {                            position: 'top',                        },                        tooltip: {                            enabled: true,                        }                    }                }            });        }    });    // end: Charts    $.ajax({        url: '../controllers/salesController.php',        type: 'POST',        dataType: 'json',        data:{action: 'totalSales'},        success: function (res){            $('#totalSale').html(`₱${res}`)        }    })})