$(document).ready(function (){    $(document).on('submit','#recipeForm',function (event){        event.preventDefault()        var formData = new FormData(this)        formData.append('action','store')        $.ajax({            url: '../controllers/recipeController.php',            type: 'post',            data: formData,            dataType: 'json',            contentType: false,            processData: false,            success: function (res){                console.log(res)                function ErrorMessage(field ,message){                    if (message){                        $(`#${field}_msg`).html(message)                        $(`#${field}`).addClass('is-invalid')                    }else {                        $(`#${field}_msg`).html('')                        $(`#${field}`).removeClass('is-invalid')                    }                }                if (res.errors){                    ErrorMessage('quantityRequired',res.errors.quantityRequired)                    ErrorMessage('productId',res.errors.productId)                    ErrorMessage('ingredientId',res.errors.ingredientId)                    return;                }                // show the success message                if (res.success === true){                    Swal.fire({                        text: res.message,                        icon: "success",                        toast: true,                        position: 'top-end',                        showConfirmButton: false,                        timer: 1000,  // Auto close after 1 second                        timerProgressBar: true  // Optional: Shows a progress bar indicating the remaining time                    });                }                if (res.success === false){                    Swal.fire({                        text: res.message,                        icon: "warning",                        toast: true,                        position: 'top-end',                        showConfirmButton: false,                        timer: 1000,  // Auto close after 1 second                        timerProgressBar: true  // Optional: Shows a progress bar indicating the remaining time                    });                }                $('#ingredientId').val('')                $('#productId').val('')                $('#quantityRequired').val('')            }        })    })    $(document).on('click','#btnEditRecipe',function (id){        $.ajax({            url: '../controllers/recipeController.php',            type: 'post',            data: {Id:  id.target.value,action: 'showBaseOnId'},            success: function (res){                $('body').append(res)                $(`#modalUpdate_recipe_${id.target.value}`).modal('show')            }        })    })    $(document).on('submit','#form_recipeUpdate',function (event){        event.preventDefault()        var dataForm = new FormData(this)        dataForm.append('action','update')        $.ajax({            url: '../controllers/recipeController.php',            type: 'post',            data: dataForm,            processData: false,            contentType: false,            dataType: 'json',            success: function (res){                // show the success message                if (res.success === true){                    Swal.fire({                        text: res.message,                        icon: "success",                        iconColor: '#6610F2',                        showConfirmButton: false,                    });                    setTimeout(()=>{window.location.reload()},1000)                }                if (res.success === false){                    Swal.fire({                        text: res.message,                        icon: "warning",                        iconColor: '#6610F2',                        showConfirmButton: true,                    });                }            }        })    })    $(document).on('click','#btnArchive',function (recipeId){        Swal.fire({            title: "Are you sure?",            text: "You won't be able to revert this!",            icon: "warning",            showCancelButton: true,            confirmButtonColor: "#6610F2",            cancelButtonColor: "#d33",            confirmButtonText: "Yes, delete it!"        }).then((result) => {            if (result.isConfirmed) {                // ajax                $.ajax({                    url: '../controllers/recipeController.php',                    type: 'post',                    data:{                        recipeId: recipeId.target.value,                        action: 'archive'                    },                    dataType: 'json',                    success: function (res){                        if (res.success === true){                            Swal.fire({                                text: res.message,                                icon: "success",                                iconColor: '#6610F2',                                showConfirmButton: false                            });                            setTimeout(() =>{window.location.reload()},1000)                        }                        if (res.success === false){                            Swal.fire({                                text: res.message,                                icon: "error",                                iconColor: '#6610F2',                            });                        }                    }                })            }        });    })    //counted category    $.ajax({        url: '../controllers/recipeController.php',        type: 'post',        data:{action: 'counted'},        dataType:'json',        success:function (res){            $('#countedRecipe').html(res)        }    })})